# 1. Product Overview

## 1.1. 서비스 이름(가칭)

* **“VAK Finder” – 수능 이후 고3을 위한 학습·진로 성향 진단 서비스**

## 1.2. 목적

* 고3(특히 수능 직후) 학생들이 **본인의 정보처리 방식(V / A / K / D)** 을 이해하고
  → **학습 전략, 대학생활 루틴, 전공·진로 방향에 대한 맞춤형 가이드**를 얻을 수 있는 웹 서비스.

## 1.3. 핵심 컨셉

* 기존 종이 테스트로 사용되던 **“선호 표상 체계 검사 (Favored Representational Systems)”**를
  **웹 기반 자동 채점 + 인사이트 리포트**로 재구현. 
* 결과는 **4가지 척도**로 제공:

  * V: Visual (시각형)
  * A: Auditory (청각형)
  * K: Kinesthetic (신체감각형)
  * D: Digital/Logical (논리형)

---

# 2. 목표 & 성공 지표

## 2.1. 비즈니스/교육 목표

* 고3 대상 특강·진로수업에서 활용 가능한 **무료 웹 진단 도구** 제공.
* 학생이 결과지를 보고 **“내가 어떤 방식으로 배우고, 어떤 전공·공부법이 맞는지”** 직관적으로 이해.

## 2.2. 성공 지표(예시)

* 1차 릴리즈 후:

  * 하루 평균 진단 완료 수 50건 이상
  * 검사 완료율(시작 대비 결과 페이지 도달 비율) 80% 이상
  * 결과 공유 버튼 클릭률 30% 이상

---

# 3. 타깃 유저

## 3.1. 1차 타깃

* **수능을 끝낸 고3 학생**

  * 스마트폰으로 접속 (모바일 비중 80% 이상 가정)
  * 진로·진학 상담, 자기소개서, 대학생활 준비에 관심

## 3.2. 2차 타깃

* 담임교사, 진로진학 상담교사, 강사
  → 이후 “강사용 대시보드”는 차기 버전에서 고려 (이번 PRD 범위에는 **포함 X**, 단 데이터 구조는 확장 가능하게 설계)

---

# 4. 범위(Scope)

## 4.1. In Scope (v1)

1. **홈 페이지 + 검사 시작 화면**
2. **10문항 VAKD 검사 UI** (문항/선택지 텍스트 하드코딩 or JSON)
3. **원 테스트와 동일한 채점 로직**
4. **결과 페이지**

   * V/A/K/D 점수 표시 (그래프 포함)
   * 주/보조 유형 설명
   * 유형별 학습법 / 대학생활 팁 / 전공 추천 간단 버전
   * 결과 공유용 썸네일 이미지 생성(프론트에서 캡처)
5. **기초 로그 저장**

   * 익명 세션 ID, 점수, 검사 시간, 디바이스 타입
6. **기본 관리 기능**

   * (관리자 페이지 없음)
   * DB에 결과만 적재, 별도 UI 없이 추후 분석용

## 4.2. Out of Scope (v1)

* 로그인/회원제
* 선생님 계정/반별 관리
* 이메일 발송, 카카오 연동 등
* MBTI 등 다른 심리검사와의 결합
* 멀티 언어 지원 (v1은 **한국어 전용**)

---

# 5. 주요 User Story

1. **학생**으로서
   “수능이 끝난 뒤, 앞으로 내가 어떤 방식으로 공부하고 어떤 전공이 맞는지 알고 싶다.
   그래서 10분 정도 안에 끝나는 간단한 검사를 통해 내 스타일과 맞는 공부법/진로 힌트를 얻고 싶다.”

2. **강사/교사**로서
   “반 학생들에게 검사 링크를 한 번에 보내고, 각자 결과를 캡처해서 가져오게 하여 수업/상담에서 활용하고 싶다.”

---

# 6. 정보 구조(IA) & 라우팅

* `/` : 홈(소개 + 검사 시작)
* `/test` : 검사 진행 화면
* `/result` : 검사 결과 페이지

  * query param 예: `?sessionId=xxxx` 또는 상태 전달
* `/privacy` : 개인정보 처리, 쿠키 안내(간단 텍스트)

---

# 7. 기능 상세

## 7.1. 홈 페이지 (`/`)

### 7.1.1. 콘텐츠

* 히어로 문구:

  * “수능은 끝났지만, 진짜 시작은 지금부터.”
  * “VAKD 진단으로 **나만의 학습·진로 스타일**을 알아보세요.”
* 간단 설명:

  * V, A, K, D가 무엇인지 1줄씩 요약
* 버튼:

  * **[검사 시작하기]** → `/test`
* 하단:

  * 사용 시간 안내: “약 5~7분 소요”
  * “개인 정보는 저장하지 않습니다. (결과 통계만 익명 수집)”

### 7.1.2. UX 요구사항

* 모바일 퍼스트, 스크롤 길이 짧게
* CTA 버튼 뚜렷하게

---

## 7.2. 검사 페이지 (`/test`)

### 7.2.1. 검사 구조

* 총 **10문항**, 각 문항에 **4개의 진술문**.
* 사용자는 각 문항에서 다음을 수행:

  * **각 진술문에 4, 3, 2, 1 중 하나의 점수를 부여**
  * 단, **한 문항 내에서는 4/3/2/1을 각각 한 번씩만 사용**.
  * (원 검사 방식 그대로 구현 – 총점 100점) 

#### 7.2.1.1. 점수 부여 UX 안

* **선호안 A (라디오 매트릭스 방식)**

  * 각 진술문 오른쪽에 [4][3][2][1] 라디오 버튼
  * 한 문항 내에서 각 숫자는 한 번만 선택 가능하도록 제약 로직
  * 선택 충돌 시 이전 선택 해제

* **대체안 B (순위 선택 방식)**

  * 진술문 카드 4개를 “가장 나와 비슷한 것(4점) → 가장 덜 비슷한 것(1점)” 순으로 드래그 정렬
  * 정렬 완료 후 자동으로 4/3/2/1 매핑
  * 구현 난이도가 올라가므로 v1에서는 A안 권장

### 7.2.2. 문항/선택지 데이터 구조

* 예시(JSON):

```ts
type Question = {
  id: number; // 1~10
  title: string; // “1. 내가 중요한 결정을 할 때…”
  options: {
    id: number; // 1~4
    text: string; // 진술문 내용
    dimension: 'V' | 'A' | 'K' | 'D'; // 아래 7.3.3 참고
  }[];
};
```

* 모든 문항/선택지는 서버 또는 프론트에 상수로 저장 (v1 하드코딩 OK).

### 7.2.3. 진행 상태

* 상단에 progress bar: `문항 n / 10`
* “이전/다음” 버튼 (문항 이동)
* 모든 문항에 4/3/2/1이 모두 채워지기 전에는 **“제출하기” 비활성화**.

### 7.2.4. 유효성 검사

* 문항 기준:

  * 각 문항에 **4개의 점수(4,3,2,1) 모두 사용** 되었는지 확인.
* 전체 기준:

  * 모든 문항 유효 → 채점 및 결과 페이지 이동.

---

# 8. 채점 로직(Assessment Engine)

## 8.1. 기본 규칙

1. 각 문항에서 4·3·2·1을 진술문에 할당.
2. 각 진술문은 하나의 차원(V/A/K/D)에 매핑됨.
3. **각 차원의 총점 = 해당 차원에 속한 진술문에 부여된 점수의 합.**
4. 전체 합은 항상 100점이 되어야 함 (검산용). 

## 8.2. 문항별 옵션 → 차원 매핑

**행 = 선택지 번호(1~4), 열 = 문항 번호(1~10)**
(PDF의 채점표를 그대로 옮긴 것) 

### 8.2.1. 매핑 테이블

| 문항 | 옵션1 | 옵션2 | 옵션3 | 옵션4 |
| -- | --- | --- | --- | --- |
| 1  | K   | A   | V   | D   |
| 2  | A   | V   | D   | K   |
| 3  | V   | K   | D   | A   |
| 4  | A   | D   | K   | V   |
| 5  | A   | D   | K   | V   |
| 6  | K   | V   | A   | D   |
| 7  | A   | V   | D   | K   |
| 8  | V   | A   | K   | D   |
| 9  | A   | D   | K   | V   |
| 10 | V   | A   | K   | D   |

→ 개발 시, Question 옵션 객체에 `dimension` 필드로 이 값들을 설정.

## 8.3. 주/보조 유형 판정

1. V/A/K/D 점수 계산 후 **내림차순 정렬**.
2. 최대 점수 유형 → **Primary Type**
3. 2번째 유형 → **Secondary Type**
4. 동점 처리:

   * 동점이 있을 경우,

     * UI 상에서 “공동 1위 / 공동 2위”를 표기
     * 설명 텍스트는 2개 유형을 모두 보여줌.

```ts
type Result = {
  scores: { V: number; A: number; K: number; D: number };
  primary: 'V' | 'A' | 'K' | 'D'[];
  secondary: 'V' | 'A' | 'K' | 'D'[];
};
```

---

# 9. 결과 페이지(`/result`) 상세

## 9.1. 데이터 입력

* 프론트에서 `Result` 객체를 생성한 뒤,

  * URL state / router state를 통해 결과 페이지로 전달
  * or 전역 상태 관리 (Redux/Zustand 등) 사용
* 선택적으로 서버에 결과 POST (9.4. API 참고)

## 9.2. 구성 요소

1. **헤더**

   * “당신의 학습·진로 스타일 결과”
   * 간단 서브타이틀:
     “점수는 성격을 ‘줄 세우기’가 아니라, **정보를 어떻게 처리하는지에 대한 경향**을 보여줘요.”

2. **V/A/K/D 점수 시각화**

   * Radar Chart or Bar Chart
   * 예: `chart.js` 또는 `recharts` 사용
   * 점수 labels, 툴팁(예: “시각형(V): 32점”)

3. **주/보조 유형 요약**

   예시 텍스트 구조(카피는 이후 튜닝):

   * Primary: **시각형 (Visual)**

     * “정보를 눈으로 보고 이해하는 것을 가장 선호하는 유형입니다.”
   * Secondary: **논리형 (Digital/Logical)**

     * “자료를 분석하고 구조화하는 데 강점을 가진 유형입니다.”

4. **유형별 상세 설명 블록**

   각 차원에 대해 공통 구조:

   * 제목: “V – 시각형(Visual)”
   * 3가지 섹션:

     1. **당신의 정보 처리 방식**
     2. **대학에서의 공부법 추천**
     3. **추천 전공/진로 키워드 (가벼운 수준)**

   → v1에서는 간단 텍스트 하드코딩.
   (복수 유형일 경우 두 개 유형을 강조, 나머지는 “보조 경향”으로 간단 설명만)

5. **고3 맞춤 솔루션 영역**

   각 학생에게 공통 + 유형별 TIP mix.

   예시 섹션 구조:

   * 🎓 “대학 강의 들을 때 이렇게 해보면 좋아요”
   * 🧠 “시험·과제 준비 공부법”
   * 🧭 “전공·진로 탐색 시 체크 포인트”
   * ⏰ “당신에게 맞는 하루 루틴 예시 (아침·오후·저녁)”

   → 텍스트는 유형별로 약간 달라짐 (조건부 렌더).

6. **결과 공유/저장**

   * “결과 이미지로 저장하기” 버튼

     * 결과 카드 영역을 캡처(canvas or html-to-image 라이브러리 사용)
   * “링크 복사” 버튼 (단축 URL은 v2에서 고려)

7. **재검사 안내**

   * “나중에 다시 검사해보고 비교해보세요” 문구
   * [다시 검사하기] 버튼 → `/test` 리다이렉트

## 9.3. UI/UX 요구사항

* 모바일에서 스크롤이 너무 길지 않도록 **섹션 접기/펼치기** 고려
* 그래프는 터치에 최적화
* 눈에 잘 들어오는 색상(각 유형별 고유 컬러) 사용

---

# 10. 데이터 & API

## 10.1. 데이터 모델 (v1 최소)

백엔드/DB를 둔다면(예: Supabase, Firebase, PlanetScale 등):

```sql
Table: test_results
- id (uuid, pk)
- created_at (timestamp)
- scores_v (int)
- scores_a (int)
- scores_k (int)
- scores_d (int)
- primary_types (varchar)   -- 예: "V" or "V,A"
- secondary_types (varchar) -- 예: "D"
- client_type (varchar)     -- 'mobile' / 'desktop'
- user_agent (text, optional)
```

개인 식별 정보는 저장하지 않는다.

## 10.2. API (선택적 구현)

### POST `/api/results`

* 목적: 검사 완료 결과 저장
* Request Body (JSON):

```json
{
  "scores": { "V": 30, "A": 25, "K": 20, "D": 25 },
  "primaryTypes": ["V"],
  "secondaryTypes": ["A"],
  "clientType": "mobile"
}
```

* Response:

```json
{
  "id": "uuid",
  "createdAt": "2025-12-02T02:10:00.000Z"
}
```

### GET `/api/results/:id` (옵션)

* 특정 결과 다시 조회(링크 공유용)
* v1에서는 필수 아님. (기획자 판단)

---

# 11. 기술 스택 제안

(개발 편의상 예시, 실제 선택은 자유)

* **Front-end**

  * Next.js (React, TypeScript)
  * Tailwind CSS
  * Recharts or Chart.js (그래프)

* **Back-end**

  * Next.js API Routes 또는 간단한 Node server
  * DB: Supabase / PostgreSQL / Firebase 등

* **배포**

  * Vercel / Netlify / Render 택1

---

# 12. 비기능 요구사항

* **성능**

  * 모바일 3G 환경에서도 3초 이내 초기 로딩
* **접근성**

  * 모든 입력 요소에 label 제공
  * 색각 이상 사용자를 고려해 색상 + 텍스트 병행
* **보안/개인정보**

  * 개인 식별 정보 수집 없음
  * 쿠키는 분석용 최소한만 (예: GA, self-hosted analytics)

---

# 13. Analytics & Logging

* 기본 이벤트 트래킹:

  * `test_started`
  * `test_completed`
  * `result_viewed`
  * `share_clicked`
* 추가 데이터:

  * 평균 검사 시간
  * 중도 이탈 문항 번호

---

# 14. 향후 확장(로드맵 아이디어)

v1 개발에는 포함하지 않지만, 설계 시 염두에 두기:

1. **강사용/관리자 대시보드**

   * 반별 코드로 결과 묶기
   * 유형 분포 차트
2. **MBTI / Holland 등 다른 검사와 연동**
3. **계정 기능 & 결과 히스토리**
4. **챗봇 기반 “유형별 상담” (LLM 연동)**

---

# 15. 개발 시 주의할 점 (Cursor용 메모)

1. **문항·채점 로직은 위 테이블 기준으로 정확히 구현**

   * 각 문항에서 4/3/2/1 **중복 없이** 할당되어야 함.
2. **총점 검산**

   * `V + A + K + D === 100` 이 아닐 경우 버그로 판단하고 로그 남기기.
3. **타입 판정**

   * 동점 상황 고려(배열로 리턴).
4. **모바일 UX**

   * 4개의 진술문이 한 화면에 자연스럽게 보이도록 카드형 UI 구성.
5. **텍스트 상수 분리**

   * 문항/설명은 i18n 혹은 별도 JSON으로 분리하면 향후 수정이 용이.

---